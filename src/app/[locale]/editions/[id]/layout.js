// app/[locale]/editions/[id]/layout.js

import { generateMetadata } from "./generateMetadata";

export { generateMetadata };

export default function EditionLayout({ children }) {
  return children;
}
