// app/[locale]/online/[...legacyPath]/layout.js

import { generateMetadata } from "./generateMetadata";

export { generateMetadata };

export default function OnlineArticleLayout({ children }) {
  return children;
}
